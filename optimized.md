# 待改进

这里记录一些已经实现，但可以优化的项目。
已完成的，请标记完成。

## 更多的拖拽与点击

- Task 界面下的 Kanban View 实现了拖拽功能，可以切换状态。新目标：在同一个状态内，还可以通过上下拖拽自由排序。
- Kanban View 将任务做成了卡片，点击卡片是可以编辑该任务的。发散这个优点，对于 List View 下的每一项任务，直接点击本条目也可以编辑（即：除了点击 Edit 按钮外，直接点击任务本身也可以编辑）
- 为 List View 下的任务条目，也增加拖拽，实现上下自由排序。
- 在 Dash Board 的 My Tasks 下，同样实现【点击可编辑】和【上下拖拽以排序】的效果。

## 沉浸式模式优化

- 导入更多的白噪音种类，并优化播放模式：自由选择一种声音、用户导入音声、随机播放所有

## 番茄钟与任务绑定

- 已实现对具体任务开启番茄钟（专注关联），并存入数据库。
- 改进建议：展示数据

## Dashboard 日历视图

- 当鼠标悬浮在一天上，应当展示当天的任务/专注状态，空值用“这天没有任务哦/还没有专注过哦”等用户友好的消息进行占位。

### 深色模式 (Dark Mode)
- [ ] 全局主题切换：设计一套深色 UI，包括配色方案、组件样式调整。建议在 Phase 3 中实现。

## CSS样式优化（UI 设计）
- 待定